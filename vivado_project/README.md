# Vivado Project

Open `SNN_ECG_ModelS_Unified/SNN_ECG_ModelS_Unified.xpr` if the relative source layout is preserved. If Vivado reports missing files after moving the repository, recreate the project from `scripts/create_unified_models_project.tcl` or add the files from `rtl/`, `sim/`, and `constraints/` manually.
