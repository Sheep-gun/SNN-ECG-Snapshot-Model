# AFE Demo Samples

The `mem_*.mem` files are 60-second, 1 kSPS, signed 12-bit demo ECG samples used by the analog/mixed-signal verification package.

They can be converted to analog PWL using `analog/scripts/convert_mem.py` if the Questa/XModel AFE simulation environment is available.
